SA is an asymmetric encryption algorithm, slower and best for small data or key exchanges. RSA is ideal for securely exchanging AES keys.
AES is symmetric, much faster and better for encrypting large files. AES is used for bulk encryption (e.g., HTTPS, encrypted drives).

